var marks = [40,28,75]
function setup() 

{
  createCanvas(400,400);
  var sum = marks[0 ] + marks[1] + marks[2]
  var avg = sum/marks.length
  console.log("Avg", avg);
}

function draw() 
{
background(51);

}

